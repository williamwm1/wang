# VoiceForge AI — Python dependencies
#
# System dependency (NOT installable via pip): FFmpeg (ffmpeg + ffprobe on PATH)
#   macOS:   brew install ffmpeg
#   Ubuntu:  sudo apt-get install ffmpeg
#   Windows: https://www.gyan.dev/ffmpeg/builds/  (add bin/ to PATH)

numpy>=1.24
scipy>=1.10
soundfile>=0.12
librosa>=0.10
pyloudnorm>=0.1.1

# GUI
PySide6>=6.5

# Optional, for best-quality AI de-noise (falls back to RNNoise / afftdn if
# not installed). Install manually only if you want it:
#   pip install deepfilternet
# deepfilternet>=0.5
